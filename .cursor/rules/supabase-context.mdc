---
description: Supabase infrastructure context for WK 2026 Voorspelpool
alwaysApply: true
---

# WK 2026 Voorspelpool Supabase Context

Full documentation is in `SUPABASE.md` at the project root. Read it when working with database, auth, or Realtime features.

Key facts:
- Supabase project ref: `adeactqablfksthhkbay`
- DB password: stored in connection string in SUPABASE.md
- Admin user ID: `caec526d-d38c-462b-a585-bbd2e119ed3f`
- Admin ID is hardcoded in `src/lib/constants.ts` AND in the `is_admin()` SQL function in `supabase/worldcup_schema.sql`
- Auth: magic link only (no passwords)
- Realtime enabled on: `profiles`, `matches`, `side_bets`, `app_state`
- RPC functions: `init_profile`, `propose_side_bet`, `accept_side_bet`, `cancel_side_bet`, `resolve_side_bet`, `admin_topup_tokens`, `admin_set_paid_in`, `admin_set_main_winner`, `admin_set_prediction_points`
- All UI copy is Dutch and centralised in `src/lib/i18n.ts`
- Two currencies: `prediction_points` (drives EUR pot) and `tokens` (side bets, everyone starts at 0; admin tops up via `admin_topup_tokens` after irl payment)
- Never place code comments, always stick to initial code patterns
